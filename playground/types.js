/* eslint-disable*/
/*
const name = 'wes';
const middle = "topher";
const last = `bos`;
 
const sentence = "she's so \"cool\"";
const sentence2 = `she's sp "cool"`;

//DIFFEERENT LINE BUT TOGETHER
const song = `ohh

yeh 

I like 
pizza`

/* const hello = 'hello my name is + name +'. 
"Nice to meet you";

let hello2 = 'hello my name is ';
hello2 = hello2 + name
hello2 = hello2 + Nice to meet you */

/*
const hello = `my name is ${name}. Nice to meet you. I am ${1 + 100} years old.`;

const html = ` 
    <div>
        <h2>${name}</h2>
        <p>${hello}</p>
    </div>
`;

console.log(html);
*/

/*
//NUMBERS
const smarties = 20;
const kids = 3;
const EachKidGets = Math.floor(smarties/kids);
const dadGets = smarties % kids;
console.log(`Each kid gets ${EachKidGets}`);

const price = 1034;

//OBJECTS
const person ={
    first: 'wes',
    last: 'bos',
    age: 100
};
*/
//NULL & UNDERFINED

/*
let dog;
console.log(dog)
dog = sinckers

//null
let somethingUnderfined; //is something no valuse set to it
const somethingNull = null; //is something but has a value of nothing

const cher ={
    first: 'cher'
};

const teller = {
    first: 'Raymond',
    last: 'Teller'
}

teller.first = 'Teller';
teller.last = null;
*/

//BOOLEANS is something that is ture or flase
let isDrwaing = flase;
const age = 18;
const ofAge = age > 19;
console.log(ofAge);

age = 100;
let age2 = 100;


